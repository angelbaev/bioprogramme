{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">{% trans %}Replay{% endtrans %}</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <div class="btn-group">
                        <a class="btn btn-default" href="{{ path('marketing_email_index') }}"><i class="fa fa-list"></i></a>
                    </div>    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success btn-flat" type="submit" value="{% trans %}Replay{% endtrans %}" />
    {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}
