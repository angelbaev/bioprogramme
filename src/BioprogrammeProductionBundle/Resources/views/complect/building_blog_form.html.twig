{{ form_start(form, {attr:{'id': 'ajax_building_block_new_form'}}) }}
    {{ form_widget(form) }}
    <input id="ajax_building_block_new_form_submit" class="btn btn-success btn-flat" type="submit" value="{% trans %}Save{% endtrans %}" />
{{ form_end(form) }}