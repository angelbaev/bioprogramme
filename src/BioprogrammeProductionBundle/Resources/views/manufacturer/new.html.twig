{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">{% trans %}Manufacturer creation{% endtrans %}</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <div class="btn-group">
                        <a class="btn btn-default" href="{{ path('nomenclature_manufacturer_index') }}"><i class="fa fa-list"></i></a>
                    </div>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <input class="btn btn-success btn-flat" type="submit" value="{% trans %}Create{% endtrans %}" />
    {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
